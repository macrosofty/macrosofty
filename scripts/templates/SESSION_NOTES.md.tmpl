# Session Notes — {{TODAY}}

## Summary

Project scaffolded from the Macrosofty satellite-project template.
Nothing built yet — this note exists so the session-notes chain in
`CLAUDE.md` has a starting link.

## What was generated

- `CLAUDE.md` — voice rules + principles inherited from Macrosofty;
  audience and "should/shouldn't" lists carried over.
- `README.md` — stub with placeholders for "what it does", "install",
  "run from source", "honest limitations".
- `ROADMAP.md` — Tier 1 / Tier 2 / Tier 3 / brainstorm / won't-do
  scaffold.
- `NEXT_SESSION.md` — paste-ready kickoff prompt for the next session.
- `CONTRIBUTING.md` — voice rules + PR expectations.
- `LICENSE` — Apache-2.0.
- `.gitignore` — Python + Node + common editor/OS noise.
- `SESSION_NOTES_{{TODAY}}.md` — this file.

## Decisions deferred to the first real session

- **Tech stack.** Could be a FastAPI app like AppHelperThingy, a CLI
  tool, a KDE applet, a systemd service, a library — depends on what
  the project actually is.
- **Concrete Tier 1 list.** The ROADMAP placeholders are intentional
  — first real session writes them.

## Hand-off

Next session: paste the prompt from `NEXT_SESSION.md` into Claude.
