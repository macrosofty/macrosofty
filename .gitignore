# Node / Astro (website)
website/node_modules/
website/dist/
website/.astro/
website/.cache/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment
.env
.env.local
.env.*.local
.env.production

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Build artifacts (distro — will come later)
dist/
out/
build/
*.iso

# Test screenshots (saved during QEMU tests; not source-of-truth)
Screenshot_*.png

# Whole leftover website checkout (the site moved to its own private repo)
/website/

# Publish staging output (regenerated on every publish)
/macrosofty-public-staging/

# NFS dangling files
.nfs*
